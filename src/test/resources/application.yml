kafka:
  group-id: 'merchant-history-dev'
  topic:
    merchant-details:
      receive: 'dev-merchant-details-receive-topic'
spring:
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    hibernate:
      ddl-auto: create